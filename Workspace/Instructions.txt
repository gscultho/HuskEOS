Unzip one of these configured projects and place it at the same root folder that HuskEOS is in. After opening the project, the 
contents of HuskEOS and App folders should build with zero errors and warnings. An application file app.c is provided as a baseline,
it creates three threads and starts the OS. 